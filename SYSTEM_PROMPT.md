# برومت كامل لنظام MSAA.Events (ERP)

## الفكرة العامة
ابني لي نظام ERP متكامل باسم **MSAA.Events**، شغال فعلي (Backend + Database + واجهة)، وليس مجرد mockup. النظام يخدم شركة فعاليات سعودية، ولازم يكون متوافق مع متطلبات هيئة الزكاة والضريبة (ZATCA) في الفوترة.

## التقنيات المطلوبة
- **Next.js + TypeScript** (App Router, Server Actions)
- **قاعدة بيانات NoSQL مدمجة** بدون تثبيت أي سيرفر خارجي (لا PostgreSQL ولا MongoDB حقيقي) — تخزن البيانات في ملفات محلية بنفس أسلوب MongoDB (find/insert/update)
- **Tailwind CSS** للتنسيق
- نظام تسجيل دخول بصلاحيات (admin, accountant, sales, hr, warehouse)

## الشكل العام (UI/UX)
- نمط **Oracle Fusion Cloud / Redwood**:
  - شريط تنقل علوي (Top Nav) بدل القائمة الجانبية، مع تبويبات فرعية لكل قسم
  - ألوان دافئة محايدة (خلفية كريمية) بدل الرمادي البارد
  - لون Oracle الأحمر المميز كـ accent
  - بطاقات KPI بشكل "Infotile" (أيقونة ملونة + رقم كبير)
  - زوايا دائرية، مسارات تنقل (Breadcrumbs)
- **متجاوب مع الجوال (Mobile Responsive)**: قائمة تنقل تنسدل (hamburger menu)، الجداول تتمرر أفقيًا، الفورمات تصطف عموديًا

## الوحدات (Modules)

### 1. المحاسبة (Accounting)
- شجرة الحسابات (Chart of Accounts)
- نظام فواتير بأربعة أنواع مستقلة، كل نوع له ترقيم تسلسلي خاص:
  - **فاتورة مبدئية (Proforma)** — بادئة `PRO-` — لا تُسجّل في الدفاتر المحاسبية إطلاقًا، فقط للعرض على العميل، مع زر "تحويل لفاتورة ضريبية"
  - **فاتورة ضريبية (Tax Invoice)** — بادئة `INV-` — الفاتورة الرسمية، تُرحّل للدفاتر (قيد مدين/دائن)
  - **إشعار دائن (Credit Note)** — بادئة `CN-` — يُنشأ بالرجوع لفاتورة ضريبية مرحّلة، يقلل رصيد العميل
  - **إشعار مدين (Debit Note)** — بادئة `DN-` — نفس الفكرة لكن يزيد الرصيد
- **القيود المحاسبية تُسمى "Journal Vouchers (JV)"** وليس "Journal Entries"، وترقيمها بادئة `JV-`
- تقارير مالية: الميزانية العمومية، قائمة الدخل، دفتر الأستاذ العام (يعرض كل الـ JVs)
- **كشف حساب لكل عميل (Statement of Account)**: سجل زمني بكل الفواتير الضريبية + الإشعارات + الدفعات مع رصيد متراكم (Running Balance)

### 2. المبيعات وإدارة العملاء (Sales & CRM)
- العملاء، العروض المحتملة (Leads)، أوامر البيع (Sales Orders)
- تأكيد أمر البيع يولّد فاتورة ضريبية تلقائيًا وينقص المخزون

### 3. المخزون والمشتريات (Inventory)
- المنتجات، المستودعات، حركة المخزون، الموردين، أوامر الشراء
- استلام أمر الشراء يزيد المخزون وينشئ قيد ذمم دائنة (Accounts Payable)

### 4. الموارد البشرية (HR)
- الموظفين، الأقسام، الحضور، الرواتب (تشغيل رواتب شهري ينشئ قيد صرف)

### 5. الإعدادات (Settings) — معلومات الشركة (البائع)
تُستخدم كبيانات ثابتة تُطبع في كل فاتورة ضريبية:
- الاسم بالعربي والإنجليزي
- الرقم الضريبي (VAT)
- السجل التجاري (CR)
- العنوان الوطني الكامل
- معلومات الحساب البنكي
- **شعار الشركة** (رفع صورة، يظهر في: صفحة الإعدادات، أعلى الفاتورة، الشريط العلوي للتطبيق)

## نموذج بيانات العميل (متوافق مع متطلبات الزكاة)
- **رقم عميل تسلسلي تلقائي**: `CUST-00001` وهكذا
- اسم العميل بالعربي
- اسم العميل بالإنجليزي
- الرقم الضريبي (إن وجد)
- السجل التجاري (إن وجد)
- **العنوان الوطني** بالحقول التالية بالترتيب:
  1. رقم المبنى
  2. الشارع
  3. الحي
  4. الرقم الإضافي
  5. الرمز البريدي (Zip code)
  6. المدينة
  7. الدولة — **حقل ثابت غير قابل للتعديل**، يُكتب دائمًا بكلا اللغتين: "المملكة العربية السعودية / Kingdom of Saudi Arabia"
- معلومات الاتصال: اسم المسؤول، بريده الإلكتروني، رقم الجوال
- بريد إلكتروني رئيسي للفواتير + بريد إلكتروني Info (اختياري)
- **رفع عملاء عبر ملف CSV** (استيراد جماعي بدل الإدخال اليدوي)، مع نموذج/قالب CSV قابل للتحميل

## تصميم الفاتورة الضريبية (متوافق مع ZATCA بالكامل)
يجب أن تحتوي كل فاتورة ضريبية على:
1. **معلومات الشركة (Bill From)** أعلى الصفحة: الاسم عربي/إنجليزي، الرقم الضريبي، السجل التجاري، العنوان الوطني، الشعار
2. **معلومات العميل (Bill To)**: نفس بيانات العميل + **رقم أمر الشراء (PO Number)** الخاص بهذه الفاتورة تحديدًا
3. **تفاصيل الفاتورة**:
   - تاريخ التصدير (Issue Date)
   - تاريخ التوريد (Supply Date)
   - رقم الفاتورة
   - مدة المشروع (Project Duration)
4. **جدول البنود**: وصف العملية، الكمية، السعر، **المبلغ، الضريبة، الإجمالي** — لكل سطر على حدة
5. **جدول الملخص**: المبلغ الإجمالي، الضريبة الإجمالية، الإجمالي الكلي
6. **معلومات الحساب البنكي** (اسم البنك، اسم الحساب، رقم الحساب، الآيبان)
7. **QR Code متوافق مع هيئة الزكاة (ZATCA/Fatoora)** — مُشفّر بصيغة TLV + Base64 يحتوي: اسم البائع، الرقم الضريبي، التاريخ/الوقت، الإجمالي شامل الضريبة، إجمالي الضريبة. يظهر فقط بعد ترحيل الفاتورة (ليس في حالة Draft)، ولا يظهر في الفواتير المبدئية (Proforma) لأنها ليست مستندًا ضريبيًا

## التصدير (Export)
- **تصدير Excel**: زر "Export to Excel" في صفحة الفواتير، يصدّر قائمة الفواتير حسب النوع النشط (ضريبية/مبدئية/دائن/مدين)
- **تصدير PDF**: زر "Download PDF" في كل فاتورة، ينتج PDF مطابق تمامًا لتصميم الفاتورة على الشاشة (بما فيه النص العربي بشكل صحيح والـ QR Code)

## ملاحظات تقنية مهمة (قرارات اتخذت أثناء البناء)
- قاعدة البيانات NeDB لا تدعم Prisma/PostgreSQL — بديل خفيف بدون تثبيت
- توليد PDF: جُرّب أولاً `@react-pdf/renderer` لكنه لا يدعم تشكيل الحروف العربية بشكل صحيح (مشكلة معروفة بالمكتبة) — تم الاستبدال بصفحة HTML مخصصة للطباعة + Puppeteer يشغّل Chrome الحقيقي المثبت على الجهاز لإنتاج PDF بعرض عربي سليم
- الأرقام التسلسلية لكل نوع مستند (فواتير، عملاء، قيود) تُبنى بنفس النمط: بادئة + رقم متسلسل بـ 5 خانات (مثال: `INV-00001`)

## بيانات الدخول الافتراضية
- البريد: `admin@erp.local`
- كلمة المرور: `admin123`

## الرابط
- النظام يعمل محليًا فقط حاليًا على: `http://localhost:3000` (لم يُنشر على الإنترنت بعد)
